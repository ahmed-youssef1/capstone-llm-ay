{"question_id": 44957694, "title": "Convert PipelinedRDD to dataframe", "question": "<p>I'm attempting to convert a pipelinedRDD in pyspark to a dataframe. This is the code snippet:</p>\n\n<p><code>newRDD = rdd.map(lambda row: Row(row.__fields__ + [\"tag\"])(row + (tagScripts(row), )))\ndf = newRDD.toDF()</code></p>\n\n<p>When I run the code though, I receive this error:</p>\n\n<p><code>'list' object has no attribute 'encode'</code></p>\n\n<p>I've tried multiple other combinations, such as converting it to a Pandas dataframe using:</p>\n\n<p><code>newRDD = rdd.map(lambda row: Row(row.__fields__ + [\"tag\"])(row + (tagScripts(row), )))\n df = newRDD.toPandas()</code></p>\n\n<p>But then I end up receiving this error:</p>\n\n<p><code>AttributeError: 'PipelinedRDD' object has no attribute 'toPandas'</code></p>\n\n<p>Any help would be greatly appreciated. Thank you for your time.</p>\n", "link": "https://stackoverflow.com/questions/44957694/convert-pipelinedrdd-to-dataframe", "answer_id": 44961388, "answer": "<p><code>rdd.toDF()</code> or <code>rdd.toPandas()</code> is only used for <code>SparkSession</code>.</p>\n\n<p>To fix your code, try below:</p>\n\n<pre><code>spark = SparkSession.builder.getOrCreate()\n\nrdd = spark.sparkContext.textFile()\nnewRDD = rdd.map(...)\ndf = newRDD.toDF() or newRDD.toPandas()\n</code></pre>\n"}