{"question_id": 79765912, "title": "Deserialization of multiple avro tables in Databricks", "question": "<p>I have multiple tables sent to eventhub and they're avro based with apicurio as schema registry but how can I deserialize them as pyspark's from_avro only does 1 table?</p>\n", "link": "https://stackoverflow.com/questions/79765912/deserialization-of-multiple-avro-tables-in-databricks", "answer_id": 79767368, "answer": "<p>The function <code>from_avro</code> indeed can handle only one schema (record type, not table; EventHub is not a database).</p>\n<p>To handle this, you need to split the stream first. That is, from the original EventHub DataFrame:</p>\n<ol>\n<li><p>Create a new scalar column (e.g., <code>schema_id</code>) using the bytes 1-4 of the Avro values<br />\n(byte 0 is 0, bytes 1-4 are the schema id)</p>\n</li>\n<li><p><code>groupBy()</code> the new column</p>\n</li>\n<li><p>Use <code>apply()</code>, <code>applyInPandas()</code> or <code>applyInArrow()</code> with a function that:</p>\n<ol>\n<li><p>takes two parameters: <code>schema_id</code> and <code>records</code> (a DataFrame)</p>\n</li>\n<li><p>lookup the schema based on <code>schema_id</code></p>\n</li>\n<li><p>decode the Avro values in <code>records</code></p>\n</li>\n</ol>\n</li>\n</ol>\n<p>Step 1 works for any Confluent compatible schema registry. This includes the Apicurio Registry.</p>\n"}