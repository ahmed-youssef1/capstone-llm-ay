{"question_id": 63824121, "title": "Set path file as parameter didn\u2019t work in python pyspark", "question": "<p>I want to run a code where it can ingest the data using jdbc driver and save it into a file path. It successfully ingested the data but the saving function didn\u2019t work. I know that we can use code like this to save data:</p>\n<pre><code>a.write.mode(&quot;overwrite&quot;).parquet(&quot;test/partition_test.parquet&quot;)\n</code></pre>\n<p>Is there any way I can set the file path as a parameter? I've tried to set the parameter like below but it didn\u2019t work.</p>\n<p>My code:</p>\n<pre><code>def ingest(spark, db_url, tablename, username, password,destination, driver, save_format=&quot;parquet&quot;):\n    a = spark.read.format(&quot;jdbc&quot;).option(&quot;url&quot;,db_url).option(&quot;dbtable&quot;,tablename).option(&quot;user&quot;, username).option(&quot;password&quot;,password).option(&quot;path&quot;, destination).option(&quot;driver&quot;,driver).load()   \n    return a\n\n\ningest(spark, &quot;jdbc:mysql://192.168.122.1:3306/users&quot;, &quot;users&quot;, &quot;root&quot;, &quot;123456@h21&quot;,&quot;/path&quot;, &quot;com.mysql.jdbc.Driver&quot;, save_format=&quot;parquet&quot;)\n</code></pre>\n", "link": "https://stackoverflow.com/questions/63824121/set-path-file-as-parameter-didn-t-work-in-python-pyspark", "answer_id": 63825106, "answer": "<p>You're mixing two things together in your code.  What you need to do is done in the 2 steps:</p>\n<ol>\n<li>reading the data into dataframe</li>\n<li>writing dataframe into a file</li>\n</ol>\n<p>so the code needs to be something like this:</p>\n<pre><code>def ingest(spark, db_url, tablename, username, password, destination, \n    driver, save_format=&quot;parquet&quot;):\n    a = spark.read.format(&quot;jdbc&quot;).option(&quot;url&quot;,db_url)\\\n       .option(&quot;dbtable&quot;,tablename).option(&quot;user&quot;, username)\\\n       .option(&quot;password&quot;,password).option(&quot;driver&quot;,driver).load()\n    a.write.format(save_format).save(destination)\n    return a\n</code></pre>\n<p>this function will return dataframe, but if you just need to read &amp; write data, then you can return <code>None</code> instead of dataframe.</p>\n"}