{"question_id": 78337923, "title": "How to use monotonically_increasing_id() in DuckDB", "question": "<p>I'm trying to use one of the pyspark function <code>monotonically_increasing_id()</code> through DuckDB sql query and i'm getting <code>duckdb.duckdb.CatalogException: Catalog Error: Scalar Function with name monotonically_increasing_id does not exist!</code></p>\n<p>Below is the code that i use to auto generate a number using monotonically_increasing_id() in duckDB:</p>\n<pre><code>SQL_QUERY_SRC_JOIN = &quot;SELECT monotonically_increasing_id() AS col1,a.* from (SELECT col2 FROM df_cust UNION  SELECT col3 FROM df_branch UNION SELECT col4 FROM df_rep) as a order by col1&quot;\n\nsrc_df = duckdb.sql(SQL_QUERY_SRC_JOIN).pl()\n</code></pre>\n<p>'df_' represents polars dataframe.</p>\n<p>Can someone please help me how we can implement this using duckdb?</p>\n<p>Thanks</p>\n", "link": "https://stackoverflow.com/questions/78337923/how-to-use-monotonically-increasing-id-in-duckdb", "answer_id": 79522898, "answer": "<p>I don't know exactly how <a href=\"https://spark.apache.org/docs/latest/api/python/reference/pyspark.sql/api/pyspark.sql.functions.monotonically_increasing_id.html\" rel=\"nofollow noreferrer\">monotonically_increasing_id</a> is defined, but perhaps you can implement it within DuckDB by using a <a href=\"https://duckdb.org/docs/stable/sql/functions/lambda.html\" rel=\"nofollow noreferrer\">lambda function</a> or <a href=\"https://duckdb.org/docs/stable/clients/python/function.html\" rel=\"nofollow noreferrer\">udf</a>?</p>\n<p>Something like (untested!!!):</p>\n<pre class=\"lang-py prettyprint-override\"><code>import duckdb\nfrom pyspark.sql import functions as sf\n\ndef my_best_guess() -&gt; duckdb.Type.something:\n  # Probably need to do some type hacking here?\n  return sf.monotonically_increasing_id()\n\nduckdb.create_function(my_best_guess); \n\nduckdb.sql(&quot;SELECT my_best_guess, cols FROM ...&quot;)\n</code></pre>\n<p>Sorry I don't have a complete answer, but the above looks to be pointing in the right direction.</p>\n"}