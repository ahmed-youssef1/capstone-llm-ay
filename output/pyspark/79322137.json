{"question_id": 79322137, "title": "spark.sql.extensions config item is not set in PySpark", "question": "<p>I am using AWS glue notebook and here is my Spark configuration:</p>\n<pre><code>%idle_timeout 2880\n%glue_version 4.0\n%worker_type G.1X\n%number_of_workers 2\n\nfrom pyspark import SparkContext, SparkConf\nfrom pyspark.context import SparkContext\nfrom awsglue.context import GlueContext\nfrom awsglue.job import Job\nfrom pyspark.sql import SparkSession\n\nconf = SparkConf()\nconf.setAppName(&quot;MyAppName&quot;)\nconf.set('spark.sql.extensions', 'org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions')\nconf.set('spark.sql.defaultCatalog', 'glue_catalog')\nconf.set('spark.sql.catalog.glue_catalog', 'org.apache.iceberg.spark.SparkCatalog')\nconf.set('spark.sql.catalog.glue_catalog.catalog-impl', 'org.apache.iceberg.aws.glue.GlueCatalog')\nconf.set('spark.sql.catalog.glue_catalog.io-impl', 'org.apache.iceberg.aws.s3.S3FileIO')\nconf.set('spark.sql.iceberg.handle-timestamp-without-timezone', 'true')\nconf.set('spark.sql.catalog.glue_catalog.warehouse', 'S3_PATH_TO_WAREHOUSE')\nconf.set('spark.jars', 'S3_PATH_TO_JARS/bundle-2.23.5.jar,S3_PATH_TO_JARS/iceberg-spark-runtime-3.3_2.12-1.6.1.jar,S3_PATH_TO_JARS/spark-xml_2.12-0.18.0.jar')\n\nspark = SparkSession.builder.config(conf=conf).getOrCreate()\n\nsc = SparkContext.getOrCreate(conf=conf)  # tested with .getOrCreate() but no success\nglueContext = GlueContext(sc)\njob = Job(glueContext)\n\n</code></pre>\n<p><strong>The Issue</strong></p>\n<p>The &quot;spark.sql.extensions&quot; item is not set while the others are set properly. To confirm this, we can use the following code:</p>\n<pre><code>print(spark.conf.get('spark.sql.catalog.glue_catalog'))\nprint(spark.conf.get('spark.sql.extensions'))\n</code></pre>\n<p><strong>Output</strong></p>\n<pre><code>org.apache.iceberg.spark.SparkCatalog\nNone\n</code></pre>\n<p>Another way to confirm is executing a Spark procedure like the following code:</p>\n<pre><code>sql = &quot;call glue_catalog.system.rollback_to_snapshot('db.table_name', 123456)&quot;\ndf = spark.sql(sql)\n</code></pre>\n<p><strong>Output</strong></p>\n<pre><code>ParseException: \nSyntax error at or near 'call'(line 1, pos 0)\n\n== SQL ==\ncall glue_catalog.system.rollback_to_snapshot('db.table_name', 123456)\n^^^\n</code></pre>\n<p>Assume all given parameters above are valid. It means, <code>db.table_name</code> exists, and <code>123456</code> is a real <code>snapshot_id</code>.\nThe point is that it cannot run &quot;call&quot; that is an Iceberg procedure and needs that &quot;extensions&quot; item of configuration but any regular <strong>SQL</strong>, such as <strong>SELECT</strong>, <strong>ALTER TABLE</strong>, etc., can be run by this configuration.</p>\n<p>Any thoughts?</p>\n<p><strong>Update</strong></p>\n<p>To be able to reproduce the issue on AWS Glue notebook, I included the entire notebook cell contents.</p>\n", "link": "https://stackoverflow.com/questions/79322137/spark-sql-extensions-config-item-is-not-set-in-pyspark", "answer_id": 79439193, "answer": "<p><code>spark.sql.extensions</code> should be the last config value in the config block.</p>\n<p>Your Config block would look like</p>\n<pre><code>conf = SparkConf()\nconf.setAppName(&quot;MyAppName&quot;)\nconf.set('spark.sql.defaultCatalog', 'glue_catalog')\nconf.set('spark.sql.catalog.glue_catalog', 'org.apache.iceberg.spark.SparkCatalog')\nconf.set('spark.sql.catalog.glue_catalog.catalog-impl', 'org.apache.iceberg.aws.glue.GlueCatalog')\nconf.set('spark.sql.catalog.glue_catalog.io-impl', 'org.apache.iceberg.aws.s3.S3FileIO')\nconf.set('spark.sql.iceberg.handle-timestamp-without-timezone', 'true')\nconf.set('spark.sql.catalog.glue_catalog.warehouse', 'S3_PATH_TO_WAREHOUSE')\nconf.set('spark.jars', 'S3_PATH_TO_JARS/bundle-2.23.5.jar,S3_PATH_TO_JARS/iceberg-spark-runtime-3.3_2.12-1.6.1.jar,S3_PATH_TO_JARS/spark-xml_2.12-0.18.0.jar')\nconf.set('spark.sql.extensions', 'org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions')\n</code></pre>\n"}