{"question_id": 74398086, "title": "Inherited class methods return parent object. Need to make it return object of child class", "question": "<p>I am trying to check the feasibility of subclassing the PySpark DataFrame class to add extra reusable methods across my work.</p>\n<pre><code>class EnhancedDataframe(DataFrame):\n   def __init__(self, df):\n     super().__init__(df._jdf,df.sql_ctx)\n\n  def notNullCount(self,col_name):\n     return self.filter(col(col_name).isNotNull()).count()\n</code></pre>\n<p>The problem is if I use this to create new dataframes, whenever I use a parent method which returns parent type object, my new class methods won't be available anymore with that.</p>\n<p>So, how to return an object of child class for all parent methods. One option that I consider using is the <code>__getattribute__</code> function and try to return child object by enclosing it in the child class name. Do we have any other options? What I am trying to do is it feasible?</p>\n", "link": "https://stackoverflow.com/questions/74398086/inherited-class-methods-return-parent-object-need-to-make-it-return-object-of-c", "answer_id": 74398247, "answer": "<p>Interesting problem. It's easy to break polymorphism by building new instances using the class name you know (<code>pandas.DataFrame</code>) rather than the class of the current object (<code>self.__class__</code>). From the behavior, it looks like pandas does the former instead of that latter.</p>\n<p>You can make your method a function outside of the class definition that takes the dataframe to work on as a parameter.</p>\n<pre><code>def notNullCount(df, col_name):\n     return df.filter(col(col_name).isNotNull()).count()\n</code></pre>\n<p>This is a common way to add functionality to classes. But it suffers from a similar problem with polymorphism - its not easy to have further subclasses override them.</p>\n"}