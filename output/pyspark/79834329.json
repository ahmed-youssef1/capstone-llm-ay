{"question_id": 79834329, "title": "How to Join two RDDs in pyspark with nested tuples", "question": "<p>I need to join two RDDs as part of my programming assignment. The problem is that the first RDD is nested, while the other is flat. I tried different things, but nothing seemed to work. Is there any expert on PySpark that can help me?</p>\n<p>First RDD is:</p>\n<pre><code>[(('brand', 1), ('queen', 1), ('elizabeth', 1), ...),\n(('50', 1), ('worst', 1), ('habit', 2), ...),\n (('cost', 1), ('trump', 1), ('aid', 1), ..., ('hole', 1))]\n</code></pre>\n<p>Second RDD is:</p>\n<pre><code>[('brand', 1), ('queen', 3), ('elizabeth', 2), ...]\n</code></pre>\n", "link": "https://stackoverflow.com/questions/79834329/how-to-join-two-rdds-in-pyspark-with-nested-tuples", "answer_id": 79854742, "answer": "<p>The main problem is that 'join' only works on key\u2013value RDDs, but my first RDD isn\u2019t keyed. It\u2019s actually an RDD where each element represents a document, and each document contains multiple (word, count) pairs. Because of this mismatch, trying to directly join it with the second RDD will always give an empty result.</p>\n<p>Also, since I want to preserve the nested, document-level structure and only figure out how many words from each document overlap with the overall word set, using a 'join' doesn\u2019t really fit this use case.</p>\n<p>A better approach is:</p>\n<ol>\n<li><p>Extract the set of words from the second RDD</p>\n</li>\n<li><p>Broadcast it</p>\n</li>\n<li><p>For each document, count how many words overlap</p>\n</li>\n</ol>\n<p>for example :</p>\n<pre><code>global_words = set(rdd2.map(lambda x: x[0]).collect())\nbroadcast_words = sc.broadcast(global_words)\n\ndef count_overlap(document):\n    return sum(1 for word, _ in document if word in broadcast_words.value)\n\nresult = rdd1.map(lambda doc: (doc, count_overlap(doc)))\n</code></pre>\n"}