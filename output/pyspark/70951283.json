{"question_id": 70951283, "title": "Delete row from databricks table if not existing in incoming dataset", "question": "<p>I have an delta table in Databricks. Let's call it <code>df</code> for this instance. A mocked data set below.</p>\n<pre><code>df\n+---+---+----+\n## |id|label|\n## +---+----+\n## |  1| foo|  \n## |  2| bar|  \n## +---+----+\n</code></pre>\n<p>I'm using incoming data sets (let's call it <code>new_df</code>) to <code>merge</code> into <code>df</code>.</p>\n<pre><code>new_df\n+---+---+----+\n## |id|label|\n## +---+----+\n## |  2| bar|  \n## |  3| fob|  \n## +---+----+\n</code></pre>\n<p>Code I'm using to merge:</p>\n<pre><code>MERGE INTO default.df AS df\nUSING new_df\nON df.id = new_df.id\n\nWHEN MATCHED THEN \n  UPDATE SET *\n\nWHEN NOT MATCHED THEN\n    INSERT (id, label)\n    VALUES (new_df.id, new_df.label);\n</code></pre>\n<p>This works pretty well and gives me a result like this:</p>\n<pre><code>+---+---+----+\n## |id|label|\n## +---+----+\n## |  1| foo|\n## |  2| bar|  \n## |  3| fob|  \n## +---+----+\n</code></pre>\n<p>The only issue is that I'd like for row [1, foo] to be removed since the data is no longer in new_df (and is not 'active'). What would be the best way to achieve this? Please note that Databricks ANSI SQL <code>merge</code> command is limited and doesn't have options such as <code>WHEN NOT MATCHED BY SOURCE/TARGET</code>. Ideally the solution is all done in Databricks Spark SQL but I'm open to suggestions in Pyspark as well. Thanks in advance!</p>\n", "link": "https://stackoverflow.com/questions/70951283/delete-row-from-databricks-table-if-not-existing-in-incoming-dataset", "answer_id": 79546861, "answer": "<p>necro, but adding here for completeness. Delta tables support the <code>WHEN NOT MATCHED BY SOURCE</code> clause in your merge statement (<a href=\"https://docs.databricks.com/aws/en/sql/language-manual/delta-merge-into#when-not-matched-by-source\" rel=\"nofollow noreferrer\">docs</a>):</p>\n<p>example:</p>\n<pre><code>CREATE OR REPLACE TABLE df AS\n  SELECT * FROM (\n    VALUES \n        ('1', 'foo'), -- gets deleted\n        ('2', 'bar') -- stays\n    ) AS t (id, label);\n\nSELECT * from df;\n\nCREATE OR REPLACE TABLE new_df AS\n  SELECT * FROM (\n    VALUES \n        ('2', 'bar'),\n        ('3', 'fob') -- gets added\n    ) AS t (id, label);\n\nSELECT * from new_df;\n\nMERGE INTO df\nUSING new_df\n  ON df.id = new_df.id\n\nWHEN MATCHED THEN \n  UPDATE SET *\n\nWHEN NOT MATCHED THEN\n    INSERT *\n\nWHEN NOT MATCHED BY SOURCE THEN\n    DELETE; \n\nSELECT * FROM df;\n</code></pre>\n"}