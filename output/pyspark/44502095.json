{"question_id": 44502095, "title": "Summing multiple columns in Spark", "question": "<p>How can I sum multiple columns in Spark? For example, in SparkR the following code works to get the sum of one column, but if I try to get the sum of both columns in <code>df</code>, I get an error. </p>\n\n<pre><code># Create SparkDataFrame\ndf &lt;- createDataFrame(faithful)\n\n# Use agg to sum total waiting times\nhead(agg(df, totalWaiting = sum(df$waiting)))\n##This works\n\n# Use agg to sum total of waiting and eruptions\nhead(agg(df, total = sum(df$waiting, df$eruptions)))\n##This doesn't work\n</code></pre>\n\n<p>Either SparkR or PySpark code will work.</p>\n", "link": "https://stackoverflow.com/questions/44502095/summing-multiple-columns-in-spark", "answer_id": 48548422, "answer": "<p>For PySpark, if you don't want to explicitly type out the columns:</p>\n\n<pre class=\"lang-python prettyprint-override\"><code>from operator import add\nfrom functools import reduce\nnew_df = df.withColumn('total',reduce(add, [F.col(x) for x in numeric_col_list]))\n</code></pre>\n"}